<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/loginContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".ui.login.LoginActivity"
    android:background="?colorActivityBackground"
    android:orientation="vertical"
    android:gravity="center_horizontal">

    <ImageView
        android:id="@+id/logoLogin"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:src="@mipmap/ic_default_profile_round"
        app:layout_constraintTop_toTopOf="parent"/>
    <LinearLayout
        android:id="@+id/usernameLoginWrapper"
        android:background="@drawable/custom_border_light"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="70dp"
        android:layout_marginHorizontal="6dp"
        android:paddingHorizontal="5dp"
        android:orientation="horizontal"
        app:layout_constraintTop_toBottomOf="@id/logoLogin">
        <EditText
            android:drawableStart="@drawable/ic_email"
            android:padding="10dp"
            android:id="@+id/usernameLogin"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColorHint="@color/blue_dark_2"
            android:hint="@string/prompt_email_or_username"
            android:inputType="textEmailAddress"
            android:selectAllOnFocus="true"
            android:textColor="?colorSecondary"
            android:textSize="21sp"
            android:textColorHighlight="?colorSecondaryVariant" />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/passwordLoginWrapper"
        android:background="@drawable/custom_border_light"
        android:layout_marginTop="14dp"
        android:layout_marginHorizontal="6dp"
        android:paddingHorizontal="5dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintTop_toBottomOf="@id/usernameLoginWrapper">
        <EditText
            android:drawableStart="@drawable/ic_pass"
            android:padding="10dp"
            android:id="@+id/passwordLogin"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/prompt_password"
            android:textColorHint="@color/blue_dark_2"
            android:imeActionLabel="@string/action_sign_in_short"
            android:imeOptions="actionDone"
            android:inputType="textPassword"
            android:selectAllOnFocus="true"
            android:textColor="?colorSecondary"
            android:textSize="21sp"
            android:textColorHighlight="?colorSecondaryVariant" />
    </LinearLayout>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginVertical="20dp"
        android:layout_marginHorizontal="20dp"
        android:padding="2dp"
        app:layout_constraintTop_toBottomOf="@id/passwordLoginWrapper">
        <Button
            android:id="@+id/loginBtn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/action_sign_in"
            android:textSize="16sp"
            android:textColor="?colorSecondary"
            android:background="?colorPrimaryVariant"
            android:gravity="center" />
    </androidx.cardview.widget.CardView>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="60dp"
        app:layout_constraintBottom_toBottomOf="parent"
        >
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="?colorOnPrimary"
            >
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@color/white_smoked"
                android:textColor="?colorPrimaryVariant"
                android:text="@string/not_registered"
                android:textStyle="italic"
                android:textSize="20sp"
                android:layout_marginBottom="2dp"
                android:gravity="center"
                />

            <Button
                android:id="@+id/toRegisterBtn"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/to_register"
                android:textColor="?colorSecondary"
                android:textSize="15sp"
                android:gravity="center"/>
        </LinearLayout>
    </androidx.cardview.widget.CardView>
</androidx.constraintlayout.widget.ConstraintLayout>